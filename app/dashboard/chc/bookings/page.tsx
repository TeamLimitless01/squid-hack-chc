"use client";

import { ArrowLeft, CalendarDays, CheckCircle2, Clock3, Loader2, XCircle } from "lucide-react";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";

type Booking = { id: string; bookingDate: string; area: number; bookingStatus: string; vpFinalAmount: number | null; farmer: { name: string; phone: string }; chcService: { service: { name: string }; pricingUnit: string; price: number }; assignedDriver: { user: { name: string } } | null };
const statuses = ["ALL", "REQUESTED", "ACCEPTED", "REJECTED", "CANCELLED"];
const statusLabel = (status: string) => status.toLowerCase().replaceAll("_", " ");

export default function CHCBookingsPage() {
    const [bookings, setBookings] = useState<Booking[]>([]);
    const [selectedStatus, setSelectedStatus] = useState("ALL");
    const [loading, setLoading] = useState(true);
    const [error, setError] = useState("");

    useEffect(() => { fetch("/api/chc/bookings").then(async (response) => { const data = await response.json(); if (!response.ok) throw new Error(data.error || "Unable to load bookings"); setBookings(data.bookings); }).catch((err: unknown) => setError(err instanceof Error ? err.message : "Unable to load bookings")).finally(() => setLoading(false)); }, []);
    const filteredBookings = useMemo(() => selectedStatus === "ALL" ? bookings : bookings.filter((booking) => booking.bookingStatus === selectedStatus), [bookings, selectedStatus]);
    const count = (status: string) => status === "ALL" ? bookings.length : bookings.filter((booking) => booking.bookingStatus === status).length;

    return <div className="mx-auto max-w-6xl px-6 py-10 sm:px-10"><Link href="/dashboard/chc" className="flex items-center gap-2 text-sm font-semibold text-slate-500 hover:text-emerald-700"><ArrowLeft className="h-4 w-4" /> Back to overview</Link><div className="mt-10"><p className="text-sm font-semibold text-emerald-700">Booking operations</p><h2 className="mt-2 text-4xl font-black tracking-tight">Bookings</h2><p className="mt-2 text-slate-500">Review farmer requests and track each booking by status.</p></div>{error && <p className="mt-8 rounded-xl bg-rose-50 p-4 text-sm font-medium text-rose-700">{error}</p>}{loading ? <div className="mt-10 flex min-h-48 items-center justify-center rounded-2xl border border-slate-200 bg-white"><Loader2 className="h-8 w-8 animate-spin text-emerald-600" aria-label="Loading bookings" /></div> : <><div className="mt-8 flex gap-2 overflow-x-auto pb-2">{statuses.map((status) => <button key={status} onClick={() => setSelectedStatus(status)} className={`flex shrink-0 items-center gap-2 rounded-xl px-4 py-3 text-sm font-bold capitalize transition ${selectedStatus === status ? "bg-emerald-600 text-white" : "bg-white text-slate-600 ring-1 ring-slate-200 hover:text-emerald-700"}`}>{statusLabel(status)} <span className="rounded-full bg-black/10 px-2 py-0.5 text-xs">{count(status)}</span></button>)}</div><div className="mt-4 space-y-4">{filteredBookings.map((booking) => <article key={booking.id} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-start"><div><p className="text-xs font-bold uppercase tracking-wider text-slate-400">{booking.chcService.service.name}</p><h3 className="mt-2 text-xl font-bold">{booking.farmer.name}</h3><p className="mt-1 text-sm text-slate-500">{booking.farmer.phone}</p></div><span className={`flex w-fit items-center gap-2 rounded-full px-3 py-1 text-xs font-bold capitalize ${booking.bookingStatus === "ACCEPTED" ? "bg-emerald-50 text-emerald-700" : booking.bookingStatus === "REJECTED" || booking.bookingStatus === "CANCELLED" ? "bg-rose-50 text-rose-700" : "bg-amber-50 text-amber-700"}`}>{booking.bookingStatus === "ACCEPTED" ? <CheckCircle2 className="h-4 w-4" /> : booking.bookingStatus === "REJECTED" || booking.bookingStatus === "CANCELLED" ? <XCircle className="h-4 w-4" /> : <Clock3 className="h-4 w-4" />}{statusLabel(booking.bookingStatus)}</span></div><div className="mt-5 grid gap-4 border-t border-slate-100 pt-4 text-sm sm:grid-cols-4"><p><span className="block text-xs font-bold uppercase tracking-wider text-slate-400">Date</span><span className="mt-1 flex items-center gap-1 font-semibold"><CalendarDays className="h-4 w-4 text-emerald-600" />{new Date(booking.bookingDate).toLocaleDateString()}</span></p><p><span className="block text-xs font-bold uppercase tracking-wider text-slate-400">Area</span><span className="mt-1 block font-semibold">{booking.area} acres</span></p><p><span className="block text-xs font-bold uppercase tracking-wider text-slate-400">Quoted amount</span><span className="mt-1 block font-semibold">{booking.vpFinalAmount !== null ? `₹${booking.vpFinalAmount.toLocaleString("en-IN")}` : `₹${booking.chcService.price.toLocaleString("en-IN")} / ${booking.chcService.pricingUnit.toLowerCase()}`}</span></p><p><span className="block text-xs font-bold uppercase tracking-wider text-slate-400">Driver</span><span className="mt-1 block font-semibold">{booking.assignedDriver?.user.name || "Not assigned"}</span></p></div></article>)}</div>{filteredBookings.length === 0 && <div className="mt-10 rounded-2xl border border-dashed border-slate-300 bg-white px-6 py-16 text-center"><CalendarDays className="mx-auto h-10 w-10 text-slate-300" /><h3 className="mt-4 text-lg font-bold">No {selectedStatus === "ALL" ? "bookings" : statusLabel(selectedStatus) + " bookings"}</h3><p className="mt-2 text-sm text-slate-500">Bookings matching this status will appear here.</p></div>}</>}</div>;
}
